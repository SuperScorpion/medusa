package ${servicePath};

import ${entityPath}.${entityName}${entityNameSuffix};

/**
* Created by ${author} on ${now_time}
*/
public interface ${entityName}Service extends BaseService<${entityName}${entityNameSuffix}> {
}